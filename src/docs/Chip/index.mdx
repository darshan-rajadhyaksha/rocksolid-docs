---
title: Chip
description: Displays a compact label with optional avatars, icons, delete actions, and disabled state.
---

import MDXTabeWrapper from "@/components/mdx-table-wrapper.astro";
import ComponentExample from "@/components/component-example.astro";
import ChipProps from "./props/Chip.mdx";

# Chip

The Chip component displays a compact label that can represent a value,
category, status, or interactive item. Chips can include an avatar or icon,
and can optionally provide a delete action.

<ComponentExample
name="Chip/examples/basic"
title="Basic chip"
demoLink="https://stackblitz.com/edit/rocksolid-chip-basic?file=src%2FApp.tsx"
/>

## Examples

### Colors

Use the `color` prop to communicate the semantic meaning of the chip.

<ComponentExample
name="Chip/examples/colors"
title="Colors"
demoLink="https://stackblitz.com/edit/rocksolid-chip-colors?file=src%2FApp.tsx"
/>

### Variants

Use the `variant` prop to control the visual style of the chip.

<ComponentExample
name="Chip/examples/variants"
title="Variants"
demoLink="https://stackblitz.com/edit/rocksolid-chip-variants?file=src%2FApp.tsx"
/>

### Size

Use the `size` prop to control the overall size of the chip.

<ComponentExample
name="Chip/examples/size"
title="Size"
demoLink="https://stackblitz.com/edit/rocksolid-chip-sizes?file=src%2FApp.tsx"
/>

### Avatar

Use the `avatar` prop to display an avatar before the chip label.

<ComponentExample
name="Chip/examples/avatar"
title="Avatar"
demoLink="https://stackblitz.com/edit/rocksolid-chip-avatar?file=src%2FApp.tsx"
/>

Avatars are useful when a chip represents a person, account, or other
entity with a visual identity.

### Icon

Use the `icon` prop to display an icon before the chip label.

<ComponentExample
name="Chip/examples/icon"
title="Icon"
demoLink="https://stackblitz.com/edit/rocksolid-chip-icon?file=src%2FApp.tsx"
/>

### Delete

Use the `onDelete` prop to make a chip dismissible or removable.

<ComponentExample
name="Chip/examples/delete"
title="Delete"
demoLink="https://stackblitz.com/edit/rocksolid-chip-delete?file=src%2FApp.tsx"
/>

The `onDelete` callback receives the mouse event generated by the delete
action. Use it to remove the chip or perform another action.

A custom delete icon can be provided with the `deleteIcon` prop.

### Disabled

Use the `disabled` prop to prevent interaction with the chip.

<ComponentExample
name="Chip/examples/disabled"
title="Disabled"
demoLink="https://stackblitz.com/edit/rocksolid-chip-disabled?file=src%2FApp.tsx"
/>

When `disabled` is `true`, the chip is visually presented as disabled and
does not respond to interactive actions such as deleting the chip.

### Clickable

Use the `onClick` prop to make a Chip interactive. When `onClick` is
provided, the Chip becomes focusable and clickable.

<ComponentExample
  name="Chip/examples/clickable"
  title="Clickable chip"
  demoLink="https://stackblitz.com/edit/rocksolid-chip-clickable?file=src%2FApp.tsx"
/>

### Customization

Use `slotProps` to customize the delete icon and label.

<ComponentExample
name="Chip/examples/slot-props"
title="Slot props"
demoLink="https://stackblitz.com/edit/rocksolid-chip-customization?file=src%2FApp.tsx"
/>


## Accessibility

* Keep chip labels short and meaningful.
* Do not rely on color alone to communicate important information.
* Ensure icons and avatars do not obscure the meaning of the label.
* When a chip provides a delete action, ensure the action has an accessible
  name and is understandable without relying on visual appearance.
* Use sufficient color contrast between the chip content and its background.
* Use `disabled` when the chip should not be interactive.
* If a chip represents an important status, make sure the status is also
  available in an accessible text form.

## API

### Chip

The Chip component supports labels, avatars, icons, delete actions, colors,
visual variants, disabled state, and slot-level customization.

<MDXTabeWrapper>
  <ChipProps />
</MDXTabeWrapper>

## Related Components

* [Avatar](/components/avatar/) — Use Avatar when displaying standalone user
  or entity representations.
* [Badge](/components/badge/) — Use Badge for small indicators or counts
  attached to another element.
* [Typography](/components/typography/) — Use Typography to control text
  presentation and styling.
